# Variables de entorno (NUNCA subir a git)
.env
.env.local
.env.*.local

# Configuración de teléfonos (puede contener información sensible)
phone_config.json

# Credenciales de Google (NUNCA subir a git)
credentials.json
client_secret_*.json
token.json

# Archivos JSON sensibles
*.json
!package.json
!package-lock.json
!business_config.json
!bot_messages.json

# Estado del bot (se crea automáticamente)
bot_status.json
test_mode_status.json

# Dependencias
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sistema operativo
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Archivos temporales
*.tmp
*.temp
